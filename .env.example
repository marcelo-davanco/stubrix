# Port exposed on the host AND used inside the container
MOCK_PORT=8081

# Target URL for recording / proxy mode
PROXY_TARGET=https://api.example.com

# CORS allowed origins (comma-separated, or * for all)
CORS_ORIGIN=*
